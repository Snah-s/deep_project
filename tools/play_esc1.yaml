# Demo Esc.1: entregable (deliverable/) vs greedy_full_task en asymmetric_advantages.
# Correr con scripts/demo.sh 1.  agent_0 (azul) = NUESTRO modelo.
seed: 67
mode: debug
environment:
  layout_name: asymmetric_advantages
  layout_file: null
  horizon: 250
  old_dynamics: true
policies:
  agent_0:
    type: python_class
    path: deliverable/student_agent.py
    class_name: StudentAgent
    name: entregable
    config: {}
    random_action_prob: 0.0
    max_action_time_ms: 100
    invalid_action: stay
    timeout_action: stay
  agent_1:
    type: builtin
    name: greedy_full_task
    random_action_prob: 0.0
    max_action_time_ms: 100
    invalid_action: stay
    timeout_action: stay
execution:
  num_episodes: 1
  episode_seeds: [67]
  swap_agent_positions: false
observation:
  type: featurized
  include_agent_index: true
rendering:
  mode: window
  fps: 7
  window_caption: "Esc.1 asymmetric_advantages - azul=entregable vs greedy"
  save_gif: false
  gif_path: outputs/demo_esc1.gif
logging:
  output_dir: outputs/demo_esc1_run
  save_step_log: false
  save_episode_summary: true
  save_trajectory_pickle: false
