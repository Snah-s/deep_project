# requirements.txt — dependencias Python del camino principal (ver PLAN.md, sección 1)
#
# REGLA INVIOLABLE: numpy<2 SIEMPRE primero. El paquete overcooked-ai (PyPI) usa
# np.Inf, eliminado en NumPy 2.0. En Colab, reiniciar el runtime tras instalar.
#
# Uso (vertiente venv/pip):  pip install -r requirements.txt
# El lockfile exacto se genera tras el primer GATE 0 en verde: pip freeze > requirements.lock
numpy<2
overcooked-ai
stable-baselines3>=2.0
torch
gymnasium
pyyaml
tqdm
# scipy: dep transitiva de overcooked-ai (planning.search); se importa al cargar el env.
scipy
# imageio: import top-level de overcooked/src/rendering.py (lo carga el harness en cada run).
imageio
# pygame: rendering en ventana (tools/play.py). Import perezoso; opcional para entrenar.
pygame
